spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: public
    show-sql: false
    hibernate:
      ddl-auto: update

  liquibase:
    enabled: false

  sql:
    init:
      mode: never